{% set mms_agent_version = salt['pillar.get']('mongo_mms:agent_version', '1.2.1.689-1') %}
{% set mongo_mms = salt['grains.filter_by']({
    'Debian': {
        'pkgs': [
        ],
        'pkg_source': 'https://mms.mongodb.com/download/agent/automation/mongodb-mms-automation-agent-manager_{{ mms_agent_version }}_amd64.deb'
    },
    'RedHat': {
        'pkgs': [
        ],
        'pkg_source': 'https://mms.mongodb.com/download/agent/automation/mongodb-mms-automation-agent-manager-{{ mms_agent_version }}.x86_64.rpm'
    },
    'Arch': {
        'pkgs': [
        ]
    }
}, merge=salt['pillar.get']('mongo_mms:lookup')) %}
